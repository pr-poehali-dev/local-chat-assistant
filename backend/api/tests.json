{
  "tests": [
    {
      "name": "GET / — health check",
      "method": "GET",
      "path": "/",
      "expectedStatus": 200,
      "expectedBody": {"status": "ok"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET facts list — returns 200",
      "method": "GET",
      "path": "/?r=facts&a=list",
      "expectedStatus": 200,
      "bodyMatcher": "none"
    },
    {
      "name": "GET facts conflicts — returns 200",
      "method": "GET",
      "path": "/?r=facts&a=conflicts",
      "expectedStatus": 200,
      "bodyMatcher": "none"
    },
    {
      "name": "GET facts profile — returns profile object",
      "method": "GET",
      "path": "/?r=facts&a=profile",
      "expectedStatus": 200,
      "expectedBody": {"stats": {}},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET summaries list — returns 200",
      "method": "GET",
      "path": "/?r=summaries&a=list",
      "expectedStatus": 200,
      "bodyMatcher": "none"
    }
  ]
}